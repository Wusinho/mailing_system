<%= render 'home/hero' %>
<div class="container">
  <footer class="mt-2">
    <div class="row">
      <%= render 'home/list' %>

      <div class="col-6">
          <h3><%= t('home_page.index.welcome') %></h3>
          <p><%= t 'home_page.index.services_html' %></p>
          <%= render 'subscriptions/form', subscription: @subscription %>
      </div>

      <div class="d-flex justify-content-center pt-2 mt-2 border-top">
        <p>&copy; 2022 Company, Inc. All rights reserved.</p>
      </div>
    </div>
  </footer>
</div>

