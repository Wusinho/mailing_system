<div class="card text-center">
  <div class="card-header">
    <%= t 'survey.index.thanks' %>
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= t 'survey.index.survey_coupon' %></h5>
    <%= link_to t('survey.index.survey_redirection'), subscription_path(subscription.id), class: green_btn %>
  </div>
</div>
<%= render 'shared/redirect_home' %>